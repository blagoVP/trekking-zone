import { ListTreksPipe } from './list-treks.pipe';

describe('ListTreksPipe', () => {
  it('create an instance', () => {
    const pipe = new ListTreksPipe();
    expect(pipe).toBeTruthy();
  });
});
